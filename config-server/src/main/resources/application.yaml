spring:
 application:
    name: config-server
 cloud:
  config:
   server:
    git:
     uri: https://github.com/SumanSana/ecommerce-microservices
     username: ${GIT_USERNAME}
     password: ${GIT_PASSWORD}
     default-label: development
     search-paths: config-repo
     
server:
 port: 8888
