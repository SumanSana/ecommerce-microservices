spring:
  application:
    name: api-gateway
  config:
    server:
      import: optional:configserver:http://localhost:8888
      
eureka:
  client:
    service:
      url: http://localhost:8761/eureka
   
server:
  port: 8080