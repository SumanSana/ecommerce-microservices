# Port 8761 is the industry standard for Eureka
server.port=8761

spring.application.name=naming-server

# These two lines prevent the server from registering with itself
eureka.client.register-with-eureka=false
eureka.client.fetch-registry=false